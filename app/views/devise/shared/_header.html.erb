   <header class="header">
    <div class="header__bar row">
     <h2 class="grid-6"><a href="/">TOPに戻る</a></h2>
         <div class="user_nav grid-6">
          <span><%=  "#{current_user.name}さん" %></span>
         <%= link_to "マイページ", "/users/#{current_user.id}" %>
         <%= link_to "投稿する", new_tweet_path, class: "post" %>
        </div>
      </div>
    </header>