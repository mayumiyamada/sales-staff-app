<p id="notice"><%= notice %></p>

<p>
  <strong>タイトル</strong>
  <%= @calendar.title %>
</p>

<p>
  <strong>時間</strong>
  <%= @calendar.start_time.strftime("%Y-%m-%d %H:%M") %>
</p>

<p>
  <strong>内容</strong>
  <%= @calendar.content %>
</p>

<%= link_to '編集', edit_calendar_path(@calendar) %> |
<%= link_to '削除', calendar_path(@calendar.id), method: :delete %> |
<%= link_to '戻る', calendars_path %>